<header class="row">
	<!-- <h1 class="title"><a href="/"><%= event_info %></a></h1>	 -->
	<% content_for(:title) { "Locations at #{event_info.title}" } %>
	<h1>Locations</h1>
	<hr style="margin: 0 -15px">
</header>

<div id="wrapper">
	<section class="home-nav">
		<ul>
		<% @locations.each do |location| %>
			<li class="row"><%= link_to location.name, "/#{params[:event_id]}/map/#{location.id}" %></li>
		<% end %>
		</ul>
	</section>
</div>